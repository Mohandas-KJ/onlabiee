# THE FLOW

```bash
Clear Screen -> Check for project ----Y----> Attach .onlabiee/logs dir -> Create dated log file -> Start Log Mode (or) call fun "Log1" -> Write it neatly 
```

The above is the flow of the project. A basic skeleton version
